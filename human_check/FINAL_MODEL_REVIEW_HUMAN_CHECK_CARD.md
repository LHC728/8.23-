# FINAL MODEL REVIEW 人工核查卡

## 当前程序审查状态

```text
FINAL_MODEL_REVIEW_PROGRAM_VERDICT = REMEDIATION_REQUIRED
FINAL_MODEL_REVIEW_HUMAN_VERDICT = PENDING
FINAL_MODEL_FREEZE = PENDING
```

本卡不要求你替程序补证，也不要求阅读源码。当前存在一项 Q2 的证据链缺口，因此不能直接作最终通过裁决；请先判断是否接受下列事实与有界修复范围。

### 核查 1：三个 Q1 小问的结论边界是否可以接受？

【程序/审查检查了什么】

Q1(1) 保留全部几何候选；Q1(2) 保留匿名编号和远端候选；Q1(3) 只回放表 1 的固定排程。

【实际证据】

Q1(1) 覆盖镜像、多根、相切和 0/π 边界；Q1(2) 枚举 392 次身份假设并保留 294 条远端/错误候选；Q1(3) 有实际排程、信息隔离和表 1 最终几何验收。

【你需要接受的条件】

三个结论都只是局部、非退化或给定表 1 的确定性结果。

【PASS 不代表什么】

不代表 Q1 全局唯一、任意初态成功或现实飞行精度。

【我需要判断】

你是否接受：Q1 已回答题目，但论文必须保留上述局部边界？（是 / 否 / 不理解）

### 核查 2：Q2 的尺度条件是否被诚实公开？

【程序/审查检查了什么】

纯夹角的缩放负对照显示，没有尺度基线时 (d^\ast) 与 (2d^\ast) 不可区分。

【实际证据】

Q2 只使用 FY11/FY15 相距 (4d^\ast) 的可信无偏差基线注入尺度；控制器接口不接收它们的绝对坐标。

【你需要接受的条件】

FY11/FY15 可信基线是用户批准的附加条件，不是原题明示条件。

【PASS 不代表什么】

不代表纯夹角在无尺度参考时也能恢复指定间距。

【我需要判断】

你是否继续接受 Q2 仅在该可信基线条件下给出指定尺度方案？（是 / 否 / 不理解）

### 核查 3：Q2 的信息边界是否可以接受？

【程序/审查检查了什么】

控制器只接收本机两维角残差与本机试探位移；AST、事件记录和非法字段注入共同检查真值、坐标、距离、别机角和评估器输出不能进入控制器。

【实际证据】

13 架实际移动接收机均被事件审计覆盖；含真值坐标或其他接收机角的伪接口被拒绝。

【你需要接受的条件】

离线评估器可以读仿真真值，但不能把结果回灌在线动作。

【PASS 不代表什么】

不代表真实无人机已获得公共罗盘、距离或共享角度通信。

【我需要判断】

你是否接受该严格本机信息边界作为 Q2 的执行前提？（是 / 否 / 不理解）

### 核查 4：Q2 的当前缺口是什么？

【程序/审查检查了什么】

审查追溯 Q2 Gate 的实际数据流，而不是只看 `PASS` 状态。

【实际证据】

FY04/FY03 的建锚回放、11 架跟随者控制和 30 边/12 线评估目前是分开进行的；最后的几何评估直接检查理想目标格点，未检查有限步建锚和跟随者控制后的实际终点。

【你需要接受的条件】

在补上端到端回放前，Q2 只能保留局部数学、候选和分阶段回放结论。

【PASS 不代表什么】

当前 `Q2_PROGRAM_GATE = PASS` 不足以证明完整控制流程的最终 30 边/12 线误差已获验收。

【我需要判断】

你是否接受将该项列为必须修复的证据链缺口，而非直接宣布总审查通过？（是 / 否 / 不理解）

### 核查 5：是否接受有界修复范围？

【程序/审查检查了什么】

Sol 与 Terra 的结论一致：问题在验证器数据流，不在冻结数学路线或信息边界。

【实际证据】

现有 Q2 的 Jacobian、完整候选、独立根集、压力域失败和信息隔离证据均保留；缺少的是实际整条排程末态的统一验收。

【你需要接受的条件】

修复只允许：从明确扰动的完整 15 机初态执行冻结排程，把实际 FY03/FY04 终点传给 11 机本机回放，再对实际最终点验收留出角、30 边、12 线与拒绝状态；同时把可信参考误差敏感性纳入 Gate或收窄对应表述。

【PASS 不代表什么】

不授权更换 Q2 路线、增加模型、使用跨接收机角、读取新文献或启动论文写作。

【我需要判断】

你是否授权上述有界 Q2 验证器修复，再重新进行 Q2 受影响证据的人工复确认？（是 / 否 / 不理解）

## 用户最终裁决

```text
FINAL_MODEL_REVIEW_HUMAN_VERDICT = PENDING
```

只有在 MAJOR-01 修复后，用户才可给出 `PASS`、`NEEDS_CLARIFICATION` 或 `FAIL`。本卡不自动改变任何已有 Q1/Q2 人工裁决。
