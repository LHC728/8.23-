# 2022 B 题逐问解法与公式：小白友好版

> 本文介绍当前已经冻结的建模方案。它不是正式竞赛论文，而是一份帮助队友迅速理解“每一问为什么这样做、公式是什么意思、依据是什么”的说明书。

## 0. 先用一句话理解整道题

无人机不能测距离，也没有共同的全局朝向。每架接收机只能看到：

> 从自己所在的位置看，两架发射机之间张开的夹角是多少。

我们要用这些夹角完成三件事：

1. 判断接收机在哪里；
2. 发射机编号未知时，同时判断“是谁在发射”和“接收机在哪里”；
3. 让多架无人机只依靠各自测得的夹角，逐步恢复圆形或锥形编队。

当前方案采用严格的信息边界：

- 不使用距离；
- 不使用绝对坐标或共同罗盘；
- 不把一架无人机测得的夹角发给另一架无人机；
- 表 1 的真实坐标只用于离线验证，不作为无人机的实时输入；
- Q2 中“如 50 m”不是固定常数；目标间距统一写成参数 $d^\ast$，并由已批准的 FY11--FY15 可信基线提供尺度。

---

## 1. 全题共同的夹角公式

设接收机在位置 $x$，两架发射机分别在 $p_a,p_b$。从接收机指向两架发射机的向量为

```math
u=p_a-x,\qquad v=p_b-x.
```

接收机测得的夹角定义为

```math
h_{ab}(x)
=
\mathrm{atan2}
\left(
|u\times v|,
u^\mathsf T v
\right),
\qquad h_{ab}(x)\in[0,\pi].
```

公式解释：

- $u^\mathsf T v$ 是点积，反映两个方向有多“同向”；
- $u\times v$ 是二维叉积，绝对值反映两个方向张开的面积；
- $\mathrm{atan2}$ 同时利用点积和叉积，能稳定区分锐角、直角和钝角；
- 加绝对值后得到无向夹角，因此结果位于 $[0,\pi]$。

为什么不用常见的 $\arccos$？

```math
\arccos\frac{u^\mathsf T v}{\|u\|\|v\|}
```

在夹角接近 $0$ 或 $\pi$ 时容易受到浮点误差影响，而 $\mathrm{atan2}$ 通常更加稳定。但要注意：换公式只能改善数值计算，不能消除 $0$ 或 $\pi$ 本身的几何退化。

理论依据：

- 点积、叉积与平面夹角关系属于解析几何基本结论；
- 角约束能否确定位置，不能只看方程数量，还要检查局部 Jacobian 是否满秩。[Angle-Based Sensor Network Localization](https://doi.org/10.1109/TAC.2021.3061980) 提供了角约束、锚点和可固定性的理论背景，但本文的具体观测方程是根据本题独立建立的。

---

## 2. 问题 1(1)：三架发射机编号和位置都已知，怎样定位接收机？

### 2.1 先理解“定夹角圆”

假设两架发射机位于 $A,B$，接收机测得

```math
\angle AXB=\theta.
```

只知道一个夹角时，接收机 $X$ 不是一个确定点，而会落在一条圆弧上。这是圆周角定理的直接结果。

令

```math
d=\|A-B\|
```

表示两架发射机之间的内部已知距离，则候选圆半径为

```math
\rho=\frac{d}{2\sin\theta}.
```

公式解释：

- 弦 $AB$ 的长度为 $d$；
- 在同一个圆上看到同一条弦 $AB$ 的圆周角相等；
- 由弦长公式 $d=2\rho\sin\theta$，得到上式。

候选圆心位于弦 $AB$ 的垂直平分线上，圆心到弦中点的距离为

```math
d_\perp=\rho|\cos\theta|.
```

一般会有位于弦两侧的两个候选圆，因此不能只画一条圆弧。

### 2.2 三架发射机怎样把圆弧缩成有限个点？

设三架已知发射机为 $A,B,C$，接收机测得

```math
y_{AB},\qquad y_{AC},\qquad y_{BC}.
```

定位方程为

```math
\begin{cases}
h_{AB}(x)=y_{AB},\\
h_{AC}(x)=y_{AC},\\
h_{BC}(x)=y_{BC}.
\end{cases}
```

具体步骤：

1. 用 $h_{AB}(x)=y_{AB}$ 建立经过 $A,B$ 的双侧定夹角圆弧；
2. 用 $h_{AC}(x)=y_{AC}$ 建立经过 $A,C$ 的双侧定夹角圆弧；
3. 枚举两组圆弧的全部交点；
4. 用第三个夹角 $h_{BC}(x)=y_{BC}$ 删除错误分支；
5. 删除与发射机重合的非法点，并合并数值上重复的候选。

因此，本问的正确输出不是“优化器找到的第一个点”，而是候选集合

```math
\mathcal C(x)
=
\{
x:
h_{AB}(x)=y_{AB},
h_{AC}(x)=y_{AC},
h_{BC}(x)=y_{BC}
\}.
```

### 2.3 怎样判断目标位置附近是不是唯一？

从三个夹角中选择两个局部独立的夹角，组成

```math
G(x)=
\begin{bmatrix}
h_{AB}(x)-y_{AB}\\
h_{AC}(x)-y_{AC}
\end{bmatrix}.
```

计算 Jacobian

```math
J(x)=DG(x).
```

如果在接收机理想槽位 $q_r$ 附近满足

```math
\mathrm{rank}J(q_r)=2,
```

则根据反函数定理，目标点附近存在一个局部唯一的反解。

通俗解释：

- 接收机位置有横、纵两个自由度；
- 两个真正独立的角度变化必须能分别感知这两个方向；
- 如果 Jacobian 的秩只有 1，说明无论怎样解方程，仍有一个方向无法确定。

### 2.4 为什么必须保留多解检查？

三个控制点和角观测并不天然保证全局唯一。计算机视觉中的 P3P 研究已经系统展示过“角度反演可能存在多个物理解”的现象：

- [Complete Solution Classification for the Perspective-Three-Point Problem](https://doi.org/10.1109/TPAMI.2003.1217599)
- 中文补充证据见 [中文论文卡片](literature/chinese_paper_cards.md) 中《关于 P3P 问题解的唯一性条件的几点讨论》。

迁移边界：

- 我们只借鉴“先求全部候选，再判断唯一”的思想；
- 不能直接照搬 P3P 的三维方程、相机姿态变量或具体解数。

### 2.5 本问最终答案

建立双侧定夹角圆弧交会模型，完整生成候选，用第三角回代，并用 Jacobian 满秩和“位置略有偏差”的局部槽位筛选正确分支。

不能承诺任意位置全局唯一；遇到多解或退化时，应输出候选集合或拒绝定位。

---

## 3. 问题 1(2)：额外发射机编号未知，最少还需要几架？

### 3.1 为什么零架额外发射机不够？

如果只有 FY00 和 FY01 发射，接收机只能得到

```math
h_{01}(x)=y_{01}.
```

这只是一条定夹角圆弧，接收机仍然可以沿圆弧移动，所以二维位置不能唯一确定。

因此

```math
m_{\min}>0.
```

这里的 $m$ 表示除 FY00、FY01 外的额外发射机数量。

### 3.2 增加一架编号未知的发射机为什么可能够？

假设额外信号来自编号 $b$，但接收机不知道 $b$ 是谁。接收机可以得到三发射机的完整角签名

```math
\Phi_b(x)=
\begin{bmatrix}
h_{01}(x)\\
h_{0b}(x)\\
h_{1b}(x)
\end{bmatrix}.
```

虽然 $b$ 未知，但它只能来自有限的合法编号集合。因此可以逐个假设：

1. 假设匿名信号来自 FY02；
2. 调用 Q1(1) 的完整候选模型；
3. 假设来自 FY03，再求一次；
4. 依次枚举全部合法编号；
5. 检查哪一个“编号—位置”组合能同时解释全部夹角，并落在接收机自己的理想槽位附近。

这不是随机搜索，而是规模很小的完整枚举。

### 3.3 身份和位置怎样一起判断？

设接收机编号为 $r$，它的理想位置附近区域为 $U_r$。所有可行联合解写为

```math
\mathcal S_1(y)
=
\bigcup_b
\{
(b,x):
\Phi_b(x)=y,\ x\in U_r
\}.
```

如果不同身份在这个局部区域产生的观测集合彼此分开，即

```math
\Delta_{\mathrm{id}}
=
\min_{b\ne b'}
\mathrm{dist}
\left(
\Phi_b(U_r),
\Phi_{b'}(U_r)
\right)>0,
```

那么就能同时确定匿名编号和接收机位置。

公式解释：

- 每个候选编号都对应一片可能的角度观测区域；
- $\Delta_{\mathrm{id}}$ 是不同身份区域之间的最小间隔；
- 间隔大于零时，小扰动不会立即把一个编号误认成另一个编号。

### 3.4 本题有限几何核查结果

对接收机 FY02～FY09 和所有合法匿名发射机进行完整枚举，共有 56 个“接收机—匿名编号”组合。

当前预审结果为：

```math
\Delta_{\mathrm{ideal,min}}
=20^\circ
=0.3490658504\ \mathrm{rad}.
```

完整三分量 Jacobian 的最坏最小奇异值为

```math
\sigma_{\min}
=
\frac{0.215955565}{R}.
```

在本题 $R=100\ \mathrm m$ 时约为

```math
\sigma_{\min}\approx0.00215956\ \mathrm m^{-1}>0.
```

这说明所有有限理想组合在局部都保持二维可辨识。

### 3.5 最少数量结论

```math
\boxed{m_{\min}=1}
```

这句话必须带上适用条件：

- 接收机知道自己的编号和目标槽位；
- FY00、FY01 的信号身份已知；
- 匿名发射机位置无偏差；
- 接收机只是“略有偏差”，仍处于正确槽位的局部区域；
- 几何没有落在退化或分支重合位置。

如果观测落在身份边界，操作层可以增加第二架匿名发射机，再枚举两架匿名信号的所有编号排列。第二架是稳健保护，不是理论最少数量。

理论依据：

- $m=0$ 的不足来自定夹角圆弧的维数；
- $m=1$ 的充分性来自有限编号枚举、反函数定理和有限集合分离；
- 匿名方位需要把离散身份与连续几何分开处理，可参考 [Ground and Aerial Mutual Localization Using Anonymous Relative-Bearing Measurements](https://doi.org/10.1109/TRO.2016.2593454)；
- 错误关联会制造虚假交点，中文证据见 [中文论文卡片](literature/chinese_paper_cards.md) 中《被动定位系统中的方位数据关联》。

---

## 4. 问题 1(3)：怎样把表 1 的十架无人机调回圆形编队？

### 4.1 为什么不能只让 FY00 和 FY01 一直发射？

表 1 给出

```math
O=\mathrm{FY00}=(0,0),\qquad
A=\mathrm{FY01}=(100,0).
```

我们让这两架无人机保持不动，得到一条题目给定的 100 m 可信种子边。

但任何待调整无人机 $r$ 只接收 $O,A$ 时，仍只有

```math
h_{OA}(p_r)=h_{OA}(q_r),
```

其解是一条圆弧，不能确定二维位置。因此必须先建立更多可信发射点。

重要边界：

如果没有表 1 中 FY00–FY01 的可信 100 m 种子，纯夹角无法区分半径 99 m、100 m 和 101 m 的相似圆形编队。这是信息不足，而不是换一个高级算法就能解决的问题。

### 4.2 第一阶段：FY04 和 FY07 交替自举

选择

```math
B=\mathrm{FY04},\qquad C=\mathrm{FY07}.
```

它们的理想位置是

```math
B^\star=(-50,50\sqrt3),\qquad
C^\star=(-50,-50\sqrt3).
```

一个宏周期包含两个严格本地子轮。

| 子轮 | 发射机 | 接收并移动的无人机 |
|---|---|---|
| 1 | FY00、FY01、FY07 | FY04 |
| 2 | FY00、FY01、FY04 | FY07 |

两个子轮都满足“FY00 加至多三架外围机发射”的限制；实际上这里只用了 FY00 加两架外围机。

FY04 的本机残差为

```math
f_B(B;C)=
\begin{bmatrix}
\angle OBA-\pi/6\\
\angle OBC-\pi/6
\end{bmatrix}.
```

公式解释：

- 第一项要求 FY04 看 FY00、FY01 的夹角为 $30^\circ$；
- 第二项要求 FY04 看 FY00、FY07 的夹角为 $30^\circ$；
- 两个角都由 FY04 自己测量，不需要 FY07 上报任何夹角。

FY07 的本机残差为

```math
f_C(C;B)=
\begin{bmatrix}
\angle OCA-\pi/6\\
\angle OCB-\pi/6
\end{bmatrix}.
```

同理，这两个角都由 FY07 自己测量。

### 4.3 为什么这个交替过程在目标附近会收敛？

令

```math
a=\frac{1}{100\sqrt3}.
```

在理想位置处，四个偏导块为

```math
D_Bf_B=
\begin{bmatrix}
a&0\\
-a/2&-1/200
\end{bmatrix},
\qquad
D_Cf_B=
\begin{bmatrix}
0&0\\
-a&0
\end{bmatrix},
```

```math
D_Cf_C=
\begin{bmatrix}
a&0\\
-a/2&1/200
\end{bmatrix},
\qquad
D_Bf_C=
\begin{bmatrix}
0&0\\
-a&0
\end{bmatrix}.
```

两个“本机对本机位置”的 Jacobian 都可逆，说明当另一架无人机暂时固定时，本机两个角在目标附近足以确定本机二维位置。

联合 Jacobian 的数值审计结果为

```math
|\det J|=\frac{1}{1.2\times10^9}>0,
```

```math
\sigma_{\min}(J)\approx0.00256401\ \mathrm m^{-1},
\qquad
\kappa(J)\approx4.3911.
```

这些结果说明目标附近没有局部秩退化，条件数也没有异常大。

更关键的是，精确局部最佳响应满足

```math
\frac{\partial B^+}{\partial C}
=
\begin{bmatrix}
0&0\\
-2/\sqrt3&0
\end{bmatrix},
```

```math
\frac{\partial C^+}{\partial B}
=
\begin{bmatrix}
0&0\\
2/\sqrt3&0
\end{bmatrix}.
```

一个完整宏周期的线性化为

```math
\frac{\partial C^+}{\partial B}
\frac{\partial B^+}{\partial C}
=0_{2\times2}.
```

因此局部谱半径为

```math
\rho=0<1.
```

小白理解：

- 谱半径小于 1 表示小误差会在反复调整中缩小；
- 这里一阶线性误差乘积甚至为 0，说明这个 FY04/FY07 组合在理想点附近非常有利；
- 但这只证明“每个子轮充分求到本机局部根”的精确局部过程；
- 如果实际每个子轮只做一次近似小步，还必须重新做数值验证，不能直接拿 $\rho=0$ 替代实验。

理论依据：

- Jacobian 满秩与局部根来自反函数定理；
- 最佳响应对另一节点的导数来自隐函数定理；
- 离散迭代的局部稳定性由线性化谱半径判断；
- 上述矩阵和零谱结论是针对本题几何独立推导的，不是从论文直接复制的。

### 4.4 第二阶段：固定四锚，让其余六架并行调整

FY04、FY07 自举完成后，固定发射集合

```math
T=\{\mathrm{FY00},\mathrm{FY01},\mathrm{FY04},\mathrm{FY07}\}.
```

它恰好是 FY00 加三架外围机，满足题目上限。

六架接收机使用的两个主角如下。

| 接收机 | 两个本机主角 |
|---|---|
| FY02、FY03 | $h_{OA},h_{OB}$ |
| FY05、FY06 | $h_{OB},h_{OC}$ |
| FY08、FY09 | $h_{OA},h_{OC}$ |

四架发射机保持不动，所以六架接收机的方程彼此不耦合，可以同时计算和移动。

有限审计得到六个本地映射的最坏指标

```math
\sigma_{\min}\approx0.00645847\ \mathrm m^{-1},
\qquad
\kappa\approx2.36098.
```

说明目标附近的两个主角能够较稳定地识别二维位置。

### 4.5 无人机不知道全局坐标，实际怎样移动？

每架接收机沿自己的两条正交执行轴 $e_1,e_2$ 做小幅正负试探。对残差 $F_i$，用中央差分估计 Jacobian：

```math
\widehat J_i[:,\ell]
=
\frac{
F_i(u_i+\delta e_\ell)
-
F_i(u_i-\delta e_\ell)
}{2\delta},
\qquad \ell=1,2.
```

公式解释：

- 先沿本机第 $\ell$ 个方向移动 $\delta$；
- 测一次角残差；
- 再向反方向移动 $\delta$；
- 两次差值除以 $2\delta$，近似得到“沿这个方向移动时，角度变化多快”。

然后使用阻尼最小二乘步

```math
\Delta u_i
=
-\eta
\left(
\widehat J_i^\mathsf T\widehat J_i+\lambda I
\right)^{-1}
\widehat J_i^\mathsf T F_i.
```

各符号含义：

- $\Delta u_i$：本机下一次移动指令；
- $\eta$：步长系数；
- $\lambda$：阻尼，防止 Jacobian 接近奇异时移动过大；
- $I$：单位矩阵；
- $\delta,\eta,\lambda$ 都是待实验选择的算法参数，不是题目给定的物理常数。

### 4.6 怎样验证最终圆形编队？

在线控制仍只使用本机夹角。所有动作结束后，离线评估器可以读取真实坐标，检查：

1. 九架外围机到 FY00 的半径是否都接近 100 m；
2. 相邻编号的圆心角是否都接近

```math
\frac{2\pi}{9}=40^\circ;
```

3. 最大单机位置误差；
4. 每一阶段的宏周期数、总位移和失败次数。

表 1 的真实坐标只用于生成仿真观测和最后验收，不能在调整过程中直接告诉无人机“你偏了多少米”。

---

## 5. 问题 2：怎样调整成指定间距的锥形编队？

### 5.1 先把目标编队写成三角格点

把 15 架无人机排成 5 行。令行号为 $c=0,1,2,3,4$，第 $c$ 行的列号为 $j=0,1,\ldots,c$，定义内部目标坐标

```math
q_{c,j}=d^\ast
\begin{bmatrix}
-\sqrt{3}c/2\\
j-c/2
\end{bmatrix}.
```

编号按从上到下、每行从一侧到另一侧依次对应 FY01 至 FY15。这个坐标只用于研究者离线推导目标角、生成仿真观测和事后验收，不能直接输入在线控制器。

格点中三种相邻位移为

```math
\Delta q_1=d^\ast
\begin{bmatrix}0\\1\end{bmatrix},
\qquad
\Delta q_2=d^\ast
\begin{bmatrix}-\sqrt{3}/2\\-1/2\end{bmatrix},
\qquad
\Delta q_3=d^\ast
\begin{bmatrix}-\sqrt{3}/2\\1/2\end{bmatrix}.
```

它们都满足

```math
\|\Delta q_1\|=\|\Delta q_2\|=\|\Delta q_3\|=d^\ast.
```

因此，只要 15 架无人机落到这些槽位，就会自动得到三族直线，并且每条直线上的相邻间距都等于指定参数 $d^\ast$。

### 5.2 为什么纯夹角不能自己恢复指定尺度？

对全部位置同时作相似变换

```math
p_i'=sRp_i+t,
\qquad
x'=sRx+t,
\qquad
s>0,
```

其中 $R$ 表示旋转或反射，$t$ 表示平移。因为点积和二维叉积绝对值都会同时乘以 $s^2$，所以

```math
h_{ab}(x';p_a',p_b')=h_{ab}(x;p_a,p_b).
```

小白理解：把整支编队连同接收机一起放大两倍，所有夹角仍然一模一样。纯夹角只能辨认形状，不能辨认“究竟是多少米”。

为了恢复指定间距，本项目采用已经由用户批准的附加条件：FY11、FY15 是已知编号、位置无偏差且保持不动的可信种子，二者的实际基线为

```math
\|q_{11}-q_{15}\|=4d^\ast.
```

这条基线只固定共同尺度。整体平移、旋转和镜像仍然自由；FY11、FY15 也不需要向其他无人机广播绝对坐标。

必须明确：这是经批准的模型输入条件，不是题面明示事实，也不是文献自动提供的结论。如果撤回这条可信尺度条件，只能启用冻结文件中保留的自由相似尺度 fallback，不能继续声称恢复指定的 $d^\ast$。

### 5.3 总体排程：两架可信种子、两架交替建锚、十一架归槽

正式方案分三阶段：

1. 固定 FY11、FY15，利用它们保留正确尺度；
2. FY04 与 FY03 严格使用各自本机夹角交替移动，建立四参考框架；
3. 固定 FY03、FY04、FY11、FY15，其余 11 架无人机分别使用本机六个夹角并行归槽。

这里“严格本机”的意思是：哪架无人机接收，角度就只留在该机；另一架无人机不能读取、汇总或转发这个角度。

### 5.4 FY04 与 FY03 怎样交替建立参考框架？

记

```math
A=\mathrm{FY11},\qquad
B=\mathrm{FY15},\qquad
C=\mathrm{FY04},\qquad
D=\mathrm{FY03}.
```

一个宏周期包含两轮。

第一轮由 FY11、FY15、FY03 发射，只有 FY04 接收和移动。FY04 自己测得的三个目标夹角依次为

```math
\Phi_C^\ast=
\begin{bmatrix}
\pi/2\\
5\pi/6\\
\pi/3
\end{bmatrix},
```

分别对应 $h_{11,15}$、$h_{11,3}$、$h_{15,3}$。选其中两项构成主残差

```math
F_C(C;D)=
\begin{bmatrix}
h_{11,15}(C)-\pi/2\\
h_{15,3}(C)-\pi/3
\end{bmatrix},
```

剩余的

```math
h_{11,3}(C)-5\pi/6
```

作为同一接收机的留出角约束检验。它负责帮助排除错误分支，但不是另一台设备提供的独立观测。

第二轮由 FY11、FY15、FY04 发射，只有 FY03 接收和移动。FY03 的目标观测为

```math
\Phi_D^\ast=
\begin{bmatrix}
\mathrm{atan2}(2\sqrt{3},1)\\
\mathrm{atan2}(\sqrt{3},6)\\
\pi/2
\end{bmatrix},
```

分别对应 $h_{11,15}$、$h_{11,4}$、$h_{15,4}$。主残差取为

```math
F_D(D;C)=
\begin{bmatrix}
h_{11,15}(D)-\mathrm{atan2}(2\sqrt{3},1)\\
h_{11,4}(D)-\mathrm{atan2}(\sqrt{3},6)
\end{bmatrix},
```

并将

```math
h_{15,4}(D)-\pi/2
```

作为 FY03 自己的留出角约束检验。

### 5.5 无人机不知道全局坐标，怎样计算移动方向？

接收机可以沿本机两个可执行方向 $e_1,e_2$ 做很小的正负试探。对第 $\ell$ 个方向，用

```math
\widehat J_i[:,\ell]
=
\frac{F_i(u_i+\delta e_\ell)-F_i(u_i-\delta e_\ell)}{2\delta},
\qquad
\ell=1,2
```

估计“本机动作变化一点，会让本机两个角残差变化多少”。这里 $\delta$ 是本机真实执行的微小米制位移，不是测距，也不是读取自身绝对坐标。

然后计算阻尼最小二乘动作

```math
\Delta u_i
=
-\eta
\left(
\widehat J_i^{\mathsf T}\widehat J_i+\lambda I
\right)^{-1}
\widehat J_i^{\mathsf T}F_i.
```

其中：

- $\eta$ 控制动作强度；
- $\lambda$ 防止 Jacobian 接近病态时动作被过度放大；
- 最大步长限制避免一次移动过远；
- 回溯只接受能够降低本机主残差的动作。

冻结实现采用

```text
probe = 2e-4 d*
gain = 0.9
damping = 1e-7
max_step = 0.25 d*
backtracking factors = 1, 1/2, 1/4, 1/8, 1/16, 0
```

这些是已经通过 Gate 的实现参数，论文写作阶段不得另行改写。

### 5.6 为什么 FY04/FY03 在目标附近会收敛？

在目标点定义本机残差对自身位置和另一建锚机位置的偏导

```math
J_C=\frac{\partial F_C}{\partial C},
\qquad
B_C=\frac{\partial F_C}{\partial D},
\qquad
J_D=\frac{\partial F_D}{\partial D},
\qquad
B_D=\frac{\partial F_D}{\partial C}.
```

由隐函数关系，FY04 和 FY03 的局部最佳响应导数分别为

```math
F=-J_C^{-1}B_C
=
\begin{bmatrix}
-1&\sqrt{3}/3\\
-\sqrt{3}&1
\end{bmatrix},
```

```math
G=-J_D^{-1}B_D
=
\begin{bmatrix}
3/4&-\sqrt{3}/4\\
-7\sqrt{3}/4&7/4
\end{bmatrix}.
```

两者直接相乘得到

```math
GF=0_{2\times2}.
```

因此，一个完整交替宏周期在目标点的一阶误差传播满足

```math
\rho(D\mathcal T_\ast)=0.
```

同时，四维联合残差 Jacobian 的行列式为

```math
\det K=\frac{1}{117(d^\ast)^4}>0.
```

通俗地说：目标点附近没有连续的“怎么移动角度都不变”的坏方向，而且两轮交替对一阶误差的消除很强。

但必须注意：一阶谱半径为 0 不等于非线性系统两轮精确到位，也不代表任意初态都能全局收敛。正式结论只限目标邻域和非退化情况。

### 5.7 固定四参考后，剩余十一架怎样归槽？

建锚完成后固定

```math
\mathcal A_4=
\{\mathrm{FY03},\mathrm{FY04},\mathrm{FY11},\mathrm{FY15}\}.
```

四架参考机同时发射。每架剩余无人机 $i$ 只在自己的接收位置形成六维纯方位观测向量

```math
\Phi_i(x)=
\begin{bmatrix}
h_{3,4}(x)\\
h_{3,11}(x)\\
h_{3,15}(x)\\
h_{4,11}(x)\\
h_{4,15}(x)\\
h_{11,15}(x)
\end{bmatrix}.
```

这六个角虽然来自四架发射机，但都由同一架接收机测得，因此没有违反“禁止跨接收机夹角汇总”。

离线根据理想格点预装目标向量

```math
\Phi_i^\ast=\Phi_i(q_i).
```

若某一分量的目标角为 $0$ 或 $\pi$，它位于无符号角的边界，不作为普通光滑控制分量。对其余角，从所有二元组合中选择目标 Jacobian 最小奇异值最大的两项：

```math
S_i^\ast
=
\mathop{\arg\max}_{|S|=2}
\sigma_{\min}\!\left(J_{i,S}(q_i)\right).
```

最小奇异值越大，意味着本机两个动作方向越容易通过角变化区分。其余非边界角不浪费，而是用于同机回代、错误分支拒绝和异常检查。

正式选择如下。

| 接收机 | 两项主观测 |
|---|---|
| FY01 | $h_{3,11},h_{4,15}$ |
| FY02 | $h_{3,11},h_{4,15}$ |
| FY05 | $h_{3,15},h_{4,15}$ |
| FY06 | $h_{3,4},h_{4,15}$ |
| FY07 | $h_{4,15},h_{11,15}$ |
| FY08 | $h_{3,11},h_{4,15}$ |
| FY09 | $h_{3,11},h_{11,15}$ |
| FY10 | $h_{3,11},h_{11,15}$ |
| FY12 | $h_{4,11},h_{4,15}$ |
| FY13 | $h_{3,11},h_{4,15}$ |
| FY14 | $h_{3,11},h_{3,15}$ |

11 架跟随者都使用固定的同一四参考框架，彼此不需要读取对方的角或位置，所以可以并行调整。

### 5.8 为什么不能只相信数值求解器返回的第一个根？

一个定夹角约束对应弦两侧的圆弧，多个圆相交时可能出现多个候选。正式候选器因此必须：

1. 生成定夹角圆的双侧分支；
2. 保留所有圆与圆的交点；
3. 识别相切、重合和连续解族；
4. 用剩余同机夹角逐候选回代；
5. 证据不足时返回多候选或拒绝，而不是拿第一个根充当唯一解。

独立过定多初值复核器不调用生产候选器的核心求根函数，用于检查有没有因为同一代码路径而“自己证明自己”。正式结果中，当前四参考组的 11 个目标槽位均为单候选；旧参考组的负对照确实出现过双根，说明多解检测不是形式摆设。

### 5.9 最终怎样验收？

离线评估器可以读取仿真真值，但输出不得回灌在线控制。它检查：

1. 15 个节点的实际终态，而不是理想格点；
2. 30 条最近邻边是否接近 $d^\ast$；
3. 12 条最大格点直线是否共线；
4. 每架接收机自己的留出角残差；
5. 实际 FY03/FY04 建锚终点是否原样传给后续 11 架跟随者，禁止偷偷重置为理想锚点。

记第 $e$ 条最近邻边长为 $\ell_e$，平均边长为

```math
\bar\ell=\frac1{30}\sum_{e=1}^{30}\ell_e.
```

可用边长相对离散度和最大边长误差检查等距性。对一条目标直线上的点，也可用点到直线距离或叉积残差检查共线性。

正式端到端 Gate 共运行 33 个确定性案例，其中 32 个是非零扰动。所有案例都使用 FY03/FY04 的实际建锚终点继续控制，结果为 33/33 通过。最坏指标为：

| 实际终态指标 | 最坏值 | 验收阈值 |
|---|---:|---:|
| 节点误差 | $2.27\times10^{-7}d^\ast$ | $2.00\times10^{-6}d^\ast$ |
| 边长误差 | $2.70\times10^{-7}d^\ast$ | $4.00\times10^{-6}d^\ast$ |
| 边长相对标准差 | $1.17\times10^{-7}$ | 作为离散度记录 |
| 最大共线距离 | $7.43\times10^{-8}d^\ast$ | $2.00\times10^{-6}d^\ast$ |
| 最大同机留出角残差 | $1.17\times10^{-7}$ rad | $2.00\times10^{-6}$ rad |

其余关键证据包括：

- 四参考完整候选：11/11 个槽位均为单候选；
- 独立多初值复核：11/11 与生产候选器一致；
- 解析与有限差分最大导数差：$7.91\times10^{-11}$；
- 建锚周期谱半径：$1.2129\times10^{-16}$；
- 精确最佳响应回放：720/720；
- 有限试探建锚回放：256/256；
- 跟随者局部验收网格：704/704，范围为 $r\le0.20d^\ast$；
- $0.30d^\ast$ 和 $0.40d^\ast$ 压力域出现 2 个失败，因此不能外推成全局收敛；
- 给可信参考机加入 $0.001d^\ast$ 或 $0.01d^\ast$ 偏差时，严格 Gate 均失败，说明可信种子条件不可隐藏。

### 5.10 Q2 最终允许说什么？

可以说：

> 在 FY11、FY15 为已知编号且位置无偏差的可信种子、二者基线为 $4d^\ast$、FY04/FY03 位于目标邻域、每轮发射机保持不动且角几何非退化的条件下，FY04/FY03 的严格本机交替调整在目标点具有一阶零谱结构；建立四参考框架后，其余 11 个目标槽位在完整本机角观测下均为单候选，并在正式确定性端到端回放中完成等距、共线的锥形编队调整。

不可以说：

- 原题明示 FY11、FY15 无偏差；
- 没有尺度参考也能恢复指定 $d^\ast$；
- 任取三架或四架参考机都能唯一定位；
- 任意初态都全局收敛；
- 一阶零谱等于非线性两轮精确到位；
- 参考机有偏差也不影响结果；
- 仿真精度等于现实飞行精度。

---

## 6. 四问是怎样连起来的？

| 问题 | 新增加的困难 | 继承的数学骨架 |
|---|---|---|
| Q1(1) | 已知三发射机下的多解和退化 | 定夹角圆、完整候选、Jacobian |
| Q1(2) | 发射机身份未知 | Q1(1) 候选器 + 身份枚举 |
| Q1(3) | 外围发射机起初也有偏差，且禁止跨机角交换 | 本机角残差 + 严格本机交替建锚 |
| Q2 | 要恢复指定间距，目标改为三角格点 | 尺度可辨识性审计 + 本机试探 + 完整候选 + 固定参考框架 |

它们是一条逐层增加困难的模型链。Q2 复用 Q1 的观测定义、完整候选、本机有限差分和信息防火墙，但不复用圆心、100 m 半径或正九边形公式。

---

## 7. 哪些结论已经有证据，边界在哪里？

### 已经闭合的主要证据

- Q1(1)：双侧圆弧、全部交点、第三角回代、退化测试与独立复核；
- Q1(2)：零架反例、一架匿名机完整枚举、远端候选保留、高精度观测分离度和两架备用方案；
- Q1(3)：FY04/FY07 交替建锚的解析 Jacobian、局部谱、实际排程回放和信息防火墙；
- Q2：尺度不变性、FY04/FY03 一阶零谱、四参考完整候选、33 个实际端到端案例及 30 边、12 线验收。

### 仍然不能越过的边界

- 所有强结论都限定在明示的局部、非退化条件；
- 确定性仿真不是现实飞行实验；
- 可信参考机偏差会破坏 Q2 的严格结论；
- 压力域失败已经被保留，不能写成零失败或全局收敛；
- 后续论文不得改变冻结参数、正式数字或结论强度。

---

## 8. 支撑依据与文献迁移边界

### 与 Q2 直接相关的一般学术依据

1. Zhao 与 Zelazo，Bearing Rigidity and Almost Global Bearing-Only Formation Stabilization，IEEE TAC，DOI: 10.1109/TAC.2015.2459191。

   用于解释纯方位约束的平移、尺度自由度和几何刚性意识；不直接迁移其连续控制律。

2. Jing、Wan 与 Dai，Angle-Based Sensor Network Localization，IEEE TAC，DOI: 10.1109/TAC.2021.3061980。

   用于支持“角约束数量不等于唯一定位，锚点与可固定性需要单独检查”；不采用其集中式角汇总和 SDP 求解。

3. Advani 与 Weile，Position and Orientation Inference via On-board Triangulation，PLOS ONE，DOI: 10.1371/journal.pone.0180089。

   用于支持三信标条件仍可能多解、增加第四参考有助于消歧；不迁移其已知世界坐标位姿模型。

4. 刘忠，《多站纯方位定位系统的可观测性条件》。

   用于支持观测数量足够时仍需检查几何退化和秩。

5. 孙文涵、鲜斌，《基于方位信息的无人机编队控制设计与验证》，DOI: 10.7641/CTA.2023.20606。

   用于支持参考结构与自由度判断；不采用绝对位置领航、连续动力学或其 Lyapunov 结论。

6. 吴龙文等，《基于 AOA 的双机无源定位模型及其解算方法》。

   用于支持几何布局和角误差会影响纯角定位性能；不采用绝对 AOA 或双站坐标公式。

文献只支撑“为什么要检查尺度、锚点、多解、秩和几何退化”。FY11/FY15、FY04/FY03 的编号选择、$GF=0$、11/11 单候选和全部正式数值都来自本题自己的推导、有限枚举和程序 Gate，不能写成某篇论文直接证明了本题方案。

详细证据见：

- [Q2 完整解法与证据](writing_reference/Q2_DETAILED_SOLUTION_AND_EVIDENCE.md)
- [Q2 模型契约](model_contract/Q2_MODEL_CONTRACT.md)
- [Q2 正式结果](results/q2/Q2_OFFICIAL_RESULT.md)
- [最终模型审查](review/FINAL_MODEL_REVIEW.md)

---

## 9. 最后用最简单的话概括答案

- Q1(1)：一个夹角给圆弧，两个夹角产生有限交点，第三个夹角逐候选排错；不能只拿第一个根。
- Q1(2)：没有额外发射机时位置落在连续圆弧上；增加一架匿名发射机后，完整枚举其编号和几何分支，在明示局部条件下最少为 1。
- Q1(3)：固定 FY00、FY01，让 FY04、FY07 轮流只用自己的夹角建立四锚，再让其余六架分别归槽。
- Q2：用 FY11、FY15 的可信基线保留指定尺度，让 FY04、FY03 交替建立四参考框架，再让其余 11 架只用各自测得的六个夹角并行归槽。

当前冻结数学路线见 [opening/07_STRATEGY_FREEZE.md](opening/07_STRATEGY_FREEZE.md)。Q2 的正式参数、数字和结论边界分别见 [模型契约](model_contract/Q2_MODEL_CONTRACT.md)、[正式结果](results/q2/Q2_OFFICIAL_RESULT.md) 与 [最终模型审查](review/FINAL_MODEL_REVIEW.md)。
