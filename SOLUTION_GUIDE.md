# 2022 B 题逐问解法与公式：小白友好版

> 本文介绍当前已经冻结的建模方案。它不是正式竞赛论文，而是一份帮助队友迅速理解“每一问为什么这样做、公式是什么意思、依据是什么”的说明书。

## 0. 先用一句话理解整道题

无人机不能测距离，也没有共同的全局朝向。每架接收机只能看到：

> 从自己所在的位置看，两架发射机之间张开的夹角是多少。

我们要用这些夹角完成三件事：

1. 判断接收机在哪里；
2. 发射机编号未知时，同时判断“是谁在发射”和“接收机在哪里”；
3. 让多架无人机只依靠各自测得的夹角，逐步恢复圆形或锥形编队。

当前方案采用严格的信息边界：

- 不使用距离；
- 不使用绝对坐标或共同罗盘；
- 不把一架无人机测得的夹角发给另一架无人机；
- 表 1 的真实坐标只用于离线验证，不作为无人机的实时输入；
- Q2 中“如 50 m”只是例子，最终只要求所有相邻间距相等。

---

## 1. 全题共同的夹角公式

设接收机在位置 $x$，两架发射机分别在 $p_a,p_b$。从接收机指向两架发射机的向量为

$$
u=p_a-x,\qquad v=p_b-x.
$$

接收机测得的夹角定义为

$$
h_{ab}(x)
=
\operatorname{atan2}
\left(
|u\times v|,
u^\mathsf T v
\right),
\qquad h_{ab}(x)\in[0,\pi].
$$

公式解释：

- $u^\mathsf T v$ 是点积，反映两个方向有多“同向”；
- $u\times v$ 是二维叉积，绝对值反映两个方向张开的面积；
- $\operatorname{atan2}$ 同时利用点积和叉积，能稳定区分锐角、直角和钝角；
- 加绝对值后得到无向夹角，因此结果位于 $[0,\pi]$。

为什么不用常见的 $\arccos$？

$$
\arccos\frac{u^\mathsf T v}{\|u\|\|v\|}
$$

在夹角接近 $0$ 或 $\pi$ 时容易受到浮点误差影响，而 $\operatorname{atan2}$ 通常更加稳定。但要注意：换公式只能改善数值计算，不能消除 $0$ 或 $\pi$ 本身的几何退化。

理论依据：

- 点积、叉积与平面夹角关系属于解析几何基本结论；
- 角约束能否确定位置，不能只看方程数量，还要检查局部 Jacobian 是否满秩。[Angle-Based Sensor Network Localization](https://doi.org/10.1109/TAC.2021.3061980) 提供了角约束、锚点和可固定性的理论背景，但本文的具体观测方程是根据本题独立建立的。

---

## 2. 问题 1(1)：三架发射机编号和位置都已知，怎样定位接收机？

### 2.1 先理解“定夹角圆”

假设两架发射机位于 $A,B$，接收机测得

$$
\angle AXB=\theta.
$$

只知道一个夹角时，接收机 $X$ 不是一个确定点，而会落在一条圆弧上。这是圆周角定理的直接结果。

令

$$
d=\|A-B\|
$$

表示两架发射机之间的内部已知距离，则候选圆半径为

$$
\rho=\frac{d}{2\sin\theta}.
$$

公式解释：

- 弦 $AB$ 的长度为 $d$；
- 在同一个圆上看到同一条弦 $AB$ 的圆周角相等；
- 由弦长公式 $d=2\rho\sin\theta$，得到上式。

候选圆心位于弦 $AB$ 的垂直平分线上，圆心到弦中点的距离为

$$
d_\perp=\rho|\cos\theta|.
$$

一般会有位于弦两侧的两个候选圆，因此不能只画一条圆弧。

### 2.2 三架发射机怎样把圆弧缩成有限个点？

设三架已知发射机为 $A,B,C$，接收机测得

$$
y_{AB},\qquad y_{AC},\qquad y_{BC}.
$$

定位方程为

$$
\begin{cases}
h_{AB}(x)=y_{AB},\\
h_{AC}(x)=y_{AC},\\
h_{BC}(x)=y_{BC}.
\end{cases}
$$

具体步骤：

1. 用 $h_{AB}(x)=y_{AB}$ 建立经过 $A,B$ 的双侧定夹角圆弧；
2. 用 $h_{AC}(x)=y_{AC}$ 建立经过 $A,C$ 的双侧定夹角圆弧；
3. 枚举两组圆弧的全部交点；
4. 用第三个夹角 $h_{BC}(x)=y_{BC}$ 删除错误分支；
5. 删除与发射机重合的非法点，并合并数值上重复的候选。

因此，本问的正确输出不是“优化器找到的第一个点”，而是候选集合

$$
\mathcal C(x)
=
\left\{
x:
h_{AB}(x)=y_{AB},
h_{AC}(x)=y_{AC},
h_{BC}(x)=y_{BC}
\right\}.
$$

### 2.3 怎样判断目标位置附近是不是唯一？

从三个夹角中选择两个局部独立的夹角，组成

$$
G(x)=
\begin{bmatrix}
h_{AB}(x)-y_{AB}\\
h_{AC}(x)-y_{AC}
\end{bmatrix}.
$$

计算 Jacobian

$$
J(x)=DG(x).
$$

如果在接收机理想槽位 $q_r$ 附近满足

$$
\operatorname{rank}J(q_r)=2,
$$

则根据反函数定理，目标点附近存在一个局部唯一的反解。

通俗解释：

- 接收机位置有横、纵两个自由度；
- 两个真正独立的角度变化必须能分别感知这两个方向；
- 如果 Jacobian 的秩只有 1，说明无论怎样解方程，仍有一个方向无法确定。

### 2.4 为什么必须保留多解检查？

三个控制点和角观测并不天然保证全局唯一。计算机视觉中的 P3P 研究已经系统展示过“角度反演可能存在多个物理解”的现象：

- [Complete Solution Classification for the Perspective-Three-Point Problem](https://doi.org/10.1109/TPAMI.2003.1217599)
- 中文补充证据见 [中文论文卡片](literature/chinese_paper_cards.md) 中《关于 P3P 问题解的唯一性条件的几点讨论》。

迁移边界：

- 我们只借鉴“先求全部候选，再判断唯一”的思想；
- 不能直接照搬 P3P 的三维方程、相机姿态变量或具体解数。

### 2.5 本问最终答案

建立双侧定夹角圆弧交会模型，完整生成候选，用第三角回代，并用 Jacobian 满秩和“位置略有偏差”的局部槽位筛选正确分支。

不能承诺任意位置全局唯一；遇到多解或退化时，应输出候选集合或拒绝定位。

---

## 3. 问题 1(2)：额外发射机编号未知，最少还需要几架？

### 3.1 为什么零架额外发射机不够？

如果只有 FY00 和 FY01 发射，接收机只能得到

$$
h_{01}(x)=y_{01}.
$$

这只是一条定夹角圆弧，接收机仍然可以沿圆弧移动，所以二维位置不能唯一确定。

因此

$$
m_{\min}>0.
$$

这里的 $m$ 表示除 FY00、FY01 外的额外发射机数量。

### 3.2 增加一架编号未知的发射机为什么可能够？

假设额外信号来自编号 $b$，但接收机不知道 $b$ 是谁。接收机可以得到三发射机的完整角签名

$$
\Phi_b(x)=
\begin{bmatrix}
h_{01}(x)\\
h_{0b}(x)\\
h_{1b}(x)
\end{bmatrix}.
$$

虽然 $b$ 未知，但它只能来自有限的合法编号集合。因此可以逐个假设：

1. 假设匿名信号来自 FY02；
2. 调用 Q1(1) 的完整候选模型；
3. 假设来自 FY03，再求一次；
4. 依次枚举全部合法编号；
5. 检查哪一个“编号—位置”组合能同时解释全部夹角，并落在接收机自己的理想槽位附近。

这不是随机搜索，而是规模很小的完整枚举。

### 3.3 身份和位置怎样一起判断？

设接收机编号为 $r$，它的理想位置附近区域为 $U_r$。所有可行联合解写为

$$
\mathcal S_1(y)
=
\bigcup_b
\left\{
(b,x):
\Phi_b(x)=y,\ x\in U_r
\right\}.
$$

如果不同身份在这个局部区域产生的观测集合彼此分开，即

$$
\Delta_{\mathrm{id}}
=
\min_{b\ne b'}
\operatorname{dist}
\left(
\Phi_b(U_r),
\Phi_{b'}(U_r)
\right)>0,
$$

那么就能同时确定匿名编号和接收机位置。

公式解释：

- 每个候选编号都对应一片可能的角度观测区域；
- $\Delta_{\mathrm{id}}$ 是不同身份区域之间的最小间隔；
- 间隔大于零时，小扰动不会立即把一个编号误认成另一个编号。

### 3.4 本题有限几何核查结果

对接收机 FY02～FY09 和所有合法匿名发射机进行完整枚举，共有 56 个“接收机—匿名编号”组合。

当前预审结果为：

$$
\min \text{理想身份签名间隔}
=20^\circ
=0.3490658504\ \mathrm{rad}.
$$

完整三分量 Jacobian 的最坏最小奇异值为

$$
\sigma_{\min}
=
\frac{0.215955565}{R}.
$$

在本题 $R=100\ \mathrm m$ 时约为

$$
\sigma_{\min}\approx0.00215956\ \mathrm m^{-1}>0.
$$

这说明所有有限理想组合在局部都保持二维可辨识。

### 3.5 最少数量结论

$$
\boxed{m_{\min}=1}
$$

这句话必须带上适用条件：

- 接收机知道自己的编号和目标槽位；
- FY00、FY01 的信号身份已知；
- 匿名发射机位置无偏差；
- 接收机只是“略有偏差”，仍处于正确槽位的局部区域；
- 几何没有落在退化或分支重合位置。

如果观测落在身份边界，操作层可以增加第二架匿名发射机，再枚举两架匿名信号的所有编号排列。第二架是稳健保护，不是理论最少数量。

理论依据：

- $m=0$ 的不足来自定夹角圆弧的维数；
- $m=1$ 的充分性来自有限编号枚举、反函数定理和有限集合分离；
- 匿名方位需要把离散身份与连续几何分开处理，可参考 [Ground and Aerial Mutual Localization Using Anonymous Relative-Bearing Measurements](https://doi.org/10.1109/TRO.2016.2593454)；
- 错误关联会制造虚假交点，中文证据见 [中文论文卡片](literature/chinese_paper_cards.md) 中《被动定位系统中的方位数据关联》。

---

## 4. 问题 1(3)：怎样把表 1 的十架无人机调回圆形编队？

### 4.1 为什么不能只让 FY00 和 FY01 一直发射？

表 1 给出

$$
O=\mathrm{FY00}=(0,0),\qquad
A=\mathrm{FY01}=(100,0).
$$

我们让这两架无人机保持不动，得到一条题目给定的 100 m 可信种子边。

但任何待调整无人机 $r$ 只接收 $O,A$ 时，仍只有

$$
h_{OA}(p_r)=h_{OA}(q_r),
$$

其解是一条圆弧，不能确定二维位置。因此必须先建立更多可信发射点。

重要边界：

如果没有表 1 中 FY00–FY01 的可信 100 m 种子，纯夹角无法区分半径 99 m、100 m 和 101 m 的相似圆形编队。这是信息不足，而不是换一个高级算法就能解决的问题。

### 4.2 第一阶段：FY04 和 FY07 交替自举

选择

$$
B=\mathrm{FY04},\qquad C=\mathrm{FY07}.
$$

它们的理想位置是

$$
B^\star=(-50,50\sqrt3),\qquad
C^\star=(-50,-50\sqrt3).
$$

一个宏周期包含两个严格本地子轮。

| 子轮 | 发射机 | 接收并移动的无人机 |
|---|---|---|
| 1 | FY00、FY01、FY07 | FY04 |
| 2 | FY00、FY01、FY04 | FY07 |

两个子轮都满足“FY00 加至多三架外围机发射”的限制；实际上这里只用了 FY00 加两架外围机。

FY04 的本机残差为

$$
f_B(B;C)=
\begin{bmatrix}
\angle OBA-\pi/6\\
\angle OBC-\pi/6
\end{bmatrix}.
$$

公式解释：

- 第一项要求 FY04 看 FY00、FY01 的夹角为 $30^\circ$；
- 第二项要求 FY04 看 FY00、FY07 的夹角为 $30^\circ$；
- 两个角都由 FY04 自己测量，不需要 FY07 上报任何夹角。

FY07 的本机残差为

$$
f_C(C;B)=
\begin{bmatrix}
\angle OCA-\pi/6\\
\angle OCB-\pi/6
\end{bmatrix}.
$$

同理，这两个角都由 FY07 自己测量。

### 4.3 为什么这个交替过程在目标附近会收敛？

令

$$
a=\frac{1}{100\sqrt3}.
$$

在理想位置处，四个偏导块为

$$
D_Bf_B=
\begin{bmatrix}
a&0\\
-a/2&-1/200
\end{bmatrix},
\qquad
D_Cf_B=
\begin{bmatrix}
0&0\\
-a&0
\end{bmatrix},
$$

$$
D_Cf_C=
\begin{bmatrix}
a&0\\
-a/2&1/200
\end{bmatrix},
\qquad
D_Bf_C=
\begin{bmatrix}
0&0\\
-a&0
\end{bmatrix}.
$$

两个“本机对本机位置”的 Jacobian 都可逆，说明当另一架无人机暂时固定时，本机两个角在目标附近足以确定本机二维位置。

联合 Jacobian 的数值审计结果为

$$
|\det J|=\frac{1}{1.2\times10^9}>0,
$$

$$
\sigma_{\min}(J)\approx0.00256401\ \mathrm m^{-1},
\qquad
\kappa(J)\approx4.3911.
$$

这些结果说明目标附近没有局部秩退化，条件数也没有异常大。

更关键的是，精确局部最佳响应满足

$$
\frac{\partial B^+}{\partial C}
=
\begin{bmatrix}
0&0\\
-2/\sqrt3&0
\end{bmatrix},
$$

$$
\frac{\partial C^+}{\partial B}
=
\begin{bmatrix}
0&0\\
2/\sqrt3&0
\end{bmatrix}.
$$

一个完整宏周期的线性化为

$$
\frac{\partial C^+}{\partial B}
\frac{\partial B^+}{\partial C}
=0_{2\times2}.
$$

因此局部谱半径为

$$
\rho=0<1.
$$

小白理解：

- 谱半径小于 1 表示小误差会在反复调整中缩小；
- 这里一阶线性误差乘积甚至为 0，说明这个 FY04/FY07 组合在理想点附近非常有利；
- 但这只证明“每个子轮充分求到本机局部根”的精确局部过程；
- 如果实际每个子轮只做一次近似小步，还必须重新做数值验证，不能直接拿 $\rho=0$ 替代实验。

理论依据：

- Jacobian 满秩与局部根来自反函数定理；
- 最佳响应对另一节点的导数来自隐函数定理；
- 离散迭代的局部稳定性由线性化谱半径判断；
- 上述矩阵和零谱结论是针对本题几何独立推导的，不是从论文直接复制的。

### 4.4 第二阶段：固定四锚，让其余六架并行调整

FY04、FY07 自举完成后，固定发射集合

$$
T=\{\mathrm{FY00},\mathrm{FY01},\mathrm{FY04},\mathrm{FY07}\}.
$$

它恰好是 FY00 加三架外围机，满足题目上限。

六架接收机使用的两个主角如下。

| 接收机 | 两个本机主角 |
|---|---|
| FY02、FY03 | $h_{OA},h_{OB}$ |
| FY05、FY06 | $h_{OB},h_{OC}$ |
| FY08、FY09 | $h_{OA},h_{OC}$ |

四架发射机保持不动，所以六架接收机的方程彼此不耦合，可以同时计算和移动。

有限审计得到六个本地映射的最坏指标

$$
\sigma_{\min}\approx0.00645847\ \mathrm m^{-1},
\qquad
\kappa\approx2.36098.
$$

说明目标附近的两个主角能够较稳定地识别二维位置。

### 4.5 无人机不知道全局坐标，实际怎样移动？

每架接收机沿自己的两条正交执行轴 $e_1,e_2$ 做小幅正负试探。对残差 $F_i$，用中央差分估计 Jacobian：

$$
\widehat J_i[:,\ell]
=
\frac{
F_i(u_i+\delta e_\ell)
-
F_i(u_i-\delta e_\ell)
}{2\delta},
\qquad \ell=1,2.
$$

公式解释：

- 先沿本机第 $\ell$ 个方向移动 $\delta$；
- 测一次角残差；
- 再向反方向移动 $\delta$；
- 两次差值除以 $2\delta$，近似得到“沿这个方向移动时，角度变化多快”。

然后使用阻尼最小二乘步

$$
\Delta u_i
=
-\eta
\left(
\widehat J_i^\mathsf T\widehat J_i+\lambda I
\right)^{-1}
\widehat J_i^\mathsf T F_i.
$$

各符号含义：

- $\Delta u_i$：本机下一次移动指令；
- $\eta$：步长系数；
- $\lambda$：阻尼，防止 Jacobian 接近奇异时移动过大；
- $I$：单位矩阵；
- $\delta,\eta,\lambda$ 都是待实验选择的算法参数，不是题目给定的物理常数。

### 4.6 怎样验证最终圆形编队？

在线控制仍只使用本机夹角。所有动作结束后，离线评估器可以读取真实坐标，检查：

1. 九架外围机到 FY00 的半径是否都接近 100 m；
2. 相邻编号的圆心角是否都接近

$$
\frac{2\pi}{9}=40^\circ;
$$

3. 最大单机位置误差；
4. 每一阶段的宏周期数、总位移和失败次数。

表 1 的真实坐标只用于生成仿真观测和最后验收，不能在调整过程中直接告诉无人机“你偏了多少米”。

---

## 5. 问题 2：怎样调整成锥形编队？

### 5.1 先把锥形编队写成三角格点

对

$$
c=0,1,2,3,4,\qquad j=0,\ldots,c,
$$

定义编号

$$
n(c,j)=1+\frac{c(c+1)}2+j
$$

和归一化坐标

$$
q_{c,j}
=
\left(
-\frac{\sqrt3}{2}c,\,
j-\frac c2
\right).
$$

对应关系：

- 第 0 列：FY01；
- 第 1 列：FY02～FY03；
- 第 2 列：FY04～FY06；
- 第 3 列：FY07～FY10；
- 第 4 列：FY11～FY15。

相邻点之间的三类差向量是

$$
(0,1),\qquad
\left(-\frac{\sqrt3}{2},-\frac12\right),\qquad
\left(-\frac{\sqrt3}{2},\frac12\right).
$$

它们的长度都等于 1，所以这个内部坐标天然满足三族直线相邻等距。

这里取 1 只是为了方便计算。现实编队可以整体放大、缩小、平移、旋转或反射，因此不需要知道真实间距是不是 50 m。

### 5.2 第一阶段：三个外角点各自调自己的内角

选择三个外角点

$$
L=\{\mathrm{FY01},\mathrm{FY11},\mathrm{FY15}\}.
$$

对任意角点 $i$，另两个角点为 $j,k$。无人机 $i$ 自己测量三角形在本机处的内角

$$
\alpha_i=\angle jik.
$$

目标是等边三角形，所以定义角误差

$$
e_i=\alpha_i-\frac{\pi}{3}.
$$

每个角点沿自己的本机执行轴试探，估计角度对本机位置的梯度

$$
g_i=\nabla_{u_i}\alpha_i.
$$

三个角点先在同一个旧构型上完成试探并返回起点，然后在预定同步时刻各自执行

$$
u_i^+
=
u_i
-
\mu e_i
\frac{g_i}{\|g_i\|^2}.
$$

公式解释：

- 如果本机内角大于 $60^\circ$，$e_i>0$，就沿减小角度的方向移动；
- 如果本机内角小于 $60^\circ$，$e_i<0$，移动方向相反；
- 除以 $\|g_i\|^2$ 可以消除整体尺度对更新强度的一阶影响；
- 每架无人机只使用自己的角度和自己的试探结果，没有角度交换。

### 5.3 为什么三角形会在局部变成等边？

在任意边长的等边三角形附近，上述六维离散更新有六个线性化乘子：

- 四个乘子等于 1；
- 两个形状乘子等于

$$
1-\frac32\mu.
$$

要让两种形状误差缩小，需要

$$
\left|1-\frac32\mu\right|<1.
$$

解得

$$
\boxed{0<\mu<\frac43}.
$$

为什么有四个乘子始终等于 1？

因为纯夹角无法也不需要改变下列整体自由度：

1. 横向平移；
2. 纵向平移；
3. 整体旋转；
4. 共同缩放。

这四个自由度不会破坏“锥形、共线、相邻等距”，所以不是模型漏掉的信息。

可以先取

$$
\mu=\frac23
$$

作为解析上的默认值，因为此时形状乘子的一阶值为 0。但实际有限差分、较大偏差和限幅可能要求减小 $\mu$，因此仍必须做局部吸引域实验。

理论依据：

- 等边三角形的角度性质来自欧氏几何；
- 更新式属于局部梯度型迭代；
- 乘子结论来自对本题六维映射的独立线性化；
- 方位刚性文献帮助解释平移、旋转和尺度自由度，但其连续控制律并未直接使用。背景可参见 [Bearing Rigidity and Almost Global Bearing-Only Formation Stabilization](https://doi.org/10.1109/TAC.2015.2459191)。

### 5.4 第二阶段：固定三个角点，十二架无人机并行归槽

外框形成等边三角形后，让 FY01、FY11、FY15 固定并同时发射。

每个非角点无人机 $i$ 在本机处得到三角点的三对夹角签名

$$
\Psi_i(p)
=
\begin{bmatrix}
h_{1,11}(p)\\
h_{1,15}(p)\\
h_{11,15}(p)
\end{bmatrix}.
$$

在归一化目标格点上预先计算

$$
\Psi_i^\star=\Psi_i(q_i).
$$

因为夹角对平移、旋转、反射和共同缩放都不变，所以无论现实外框多大、朝向哪里，都可以使用同一套目标签名。

边界槽位可能有某个目标角等于 $0$ 或 $\pi$。这个分量不可微，因此不能作为主要控制角，只能作为单侧候选或最终共线检查。

当前选择的两个主角如下。

| 节点 | 两个主角 |
|---|---|
| FY02、FY04、FY07 | $h_{1,15},h_{11,15}$ |
| FY03、FY05、FY06、FY10 | $h_{1,11},h_{11,15}$ |
| FY08、FY09、FY12、FY13、FY14 | $h_{1,11},h_{1,15}$ |

十二架无人机的三个发射角点相同且保持不动，因此十二个二维控制问题彼此独立，可以并行执行。

### 5.5 当前有限几何检查支持什么？

十二个目标签名两两不同，最小间隔为

$$
\min_{i\ne j}
\|\Psi_i^\star-\Psi_j^\star\|
\approx0.397443\ \mathrm{rad}.
$$

避开 $0/\pi$ 分量后，十二个最佳二维角映射的最坏指标为

$$
\min_i\sigma_{\min}(DF_i)
\approx0.315524,
$$

$$
\max_i\kappa(DF_i)
\approx3.56880.
$$

这些数值说明：

- 目标槽位附近的十二个编号签名可以区分；
- 每个槽位都能找到两个局部满秩的主角；
- 但不能据此声称全平面没有远端错误根。

因此实际程序必须限制在目标局部域、完整检查候选，并在证据不足时拒绝强行归槽。

### 5.6 怎样验收最终锥形编队？

三角格点共有 30 条最近邻边。记第 $e$ 条边长度为 $\ell_e$，平均边长为

$$
\bar\ell=\frac1{30}\sum_{e=1}^{30}\ell_e.
$$

定义等距误差

$$
E_{\mathrm{eq}}
=
\max_e
\left|
\frac{\ell_e}{\bar\ell}-1
\right|.
$$

$E_{\mathrm{eq}}$ 越接近 0，说明所有相邻边越接近等长。

对每个应该共线的三点组 $(i,j,k)$，可以使用归一化叉积残差

$$
E_{\mathrm{col}}(i,j,k)
=
\frac{
|(p_j-p_i)\times(p_k-p_i)|
}{
\|p_j-p_i\|\,\|p_k-p_i\|
}.
$$

如果三点严格共线，叉积为 0，因此

$$
E_{\mathrm{col}}=0.
$$

最终报告：

- 30 条边的等距误差；
- 三族直线的最大共线残差；
- 三角点形状误差；
- 十二架跟随者的最大归一化位置误差；
- 宏周期数、并行深度和失败/拒绝次数。

---

## 6. 四问是怎样连起来的？

| 问题 | 新增加的困难 | 继承的数学骨架 |
|---|---|---|
| Q1(1) | 已知三锚下的多解和退化 | 定夹角圆、候选集、Jacobian |
| Q1(2) | 发射机身份未知 | Q1(1) 候选器 + 身份枚举 |
| Q1(3) | 外围发射机起初也有偏差，且禁止跨机角交换 | 本机角残差 + 严格本地闭环 |
| Q2 | 没有圆心和固定尺度，目标变成三角格点 | 相似规范 + 本机角反馈 + 固定外框 |

它们不是四个互不相关的小题，而是一条逐层增加困难的模型链。

---

## 7. 哪些结论已经有数学支撑，哪些还要实验？

### 已有解析或有限几何支撑

- 统一夹角公式；
- 定夹角圆半径公式；
- Q1(2) 中 $m=0$ 不足；
- Q1(2) 的 56 组合局部秩和 20° 身份间隔；
- Q1(3) 的 FY04/FY07 Jacobian、满秩和精确最佳响应周期谱半径 0；
- Q1(3) 六个后续槽位的局部满秩；
- Q2 等边点的四个规范乘子和两个形状乘子；
- Q2 十二个目标签名间隔和局部 Jacobian。

### 必须在后续程序中验证

- Q1(1) 所有圆弧分支、切点和近退化数值处理；
- Q1(2) 保守局部认证半径；
- Q1(3) 有限差分、阻尼、限幅和固定宏周期数；
- Q2 三角点控制的实际局部吸引域；
- Q2 边界槽位、远端错误根和角扰动；
- 所有 Baseline 对照和批量敏感性实验。

因此正确表述是：

> 当前核心结构和局部公式已经成立，但不能保证任意初态、任意噪声下全局唯一和全局收敛。

---

## 8. 支撑依据与文献迁移边界

### 真正改变建模判断的国际文献

1. Jing, Wan & Dai, Angle-Based Sensor Network Localization, IEEE TAC  
   https://doi.org/10.1109/TAC.2021.3061980  
   支撑角约束、锚点和局部可固定性审计。

2. Zhao & Zelazo, Bearing Rigidity and Almost Global Bearing-Only Formation Stabilization, IEEE TAC  
   https://doi.org/10.1109/TAC.2015.2459191  
   支撑形状自由度与刚性结构判断；不迁移其连续控制律。

3. Stegagno et al., Ground and Aerial Mutual Localization Using Anonymous Relative-Bearing Measurements, IEEE TRO  
   https://doi.org/10.1109/TRO.2016.2593454  
   支撑把匿名身份作为独立离散变量。

4. Gao et al., Complete Solution Classification for the Perspective-Three-Point Problem, IEEE TPAMI  
   https://doi.org/10.1109/TPAMI.2003.1217599  
   支撑有限候选和多解警示；不迁移三维 P3P 方程。

### 中文全文证据

中文论文的题录、作用和不能迁移的内容见：

- [中文论文卡片](literature/chinese_paper_cards.md)
- [综合文献证据](opening/04_LITERATURE_EVIDENCE.md)

### 最重要的证据边界

文献只能说明：

- 为什么必须检查多解；
- 为什么匿名编号必须单独建模；
- 为什么角约束数量不能替代可辨识性；
- 为什么编队要明确规范自由度和独立验证。

Q1(3) 的 FY04/FY07 零谱自举和 Q2 的三内角同步更新公式，是根据当前题目独立推导的。它们的正确性来自本题 Jacobian、隐函数和线性化计算，不能写成“某篇论文已经直接证明本题公式”。

---

## 9. 最后用最简单的话概括答案

- Q1(1)：一个夹角给一条圆弧，两个夹角让圆弧相交，第三个夹角负责排除错误点；一定要保留多解检查。
- Q1(2)：没有额外发射机时只能得到圆弧；增加一架匿名发射机后，可以枚举它的有限编号并联合判断位置，所以局部最少为 1。
- Q1(3)：先让 FY04、FY07 轮流只用自己的两个角建立可靠四锚，再让其余六架同时按自己的角签名归槽。
- Q2：先让三个外角点各自把自己的内角调到 60°，形成任意尺度的等边外框；再让十二架无人机各自按三个角点的夹角签名并行归槽。

完整的冻结施工方案见 [opening/07_STRATEGY_FREEZE.md](opening/07_STRATEGY_FREEZE.md)。
