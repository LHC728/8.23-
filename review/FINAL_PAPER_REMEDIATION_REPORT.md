# 全篇论文集中修订与 Q2 停止规则证据修复报告

## 1. 范围与结论

本轮属于冻结路线内的实现与写作修订：将 Q2 的端到端建锚排程改为固定预编排的 20 个宏周期，并将相关正式数值和论文叙述同步；未修改 `opening/07_STRATEGY_FREEZE.md`、公式含义、模型路线或任何 Q1/Q2 Gate 的结论范围。

```text
Q2_FIXED_SCHEDULE_REMEDIATION = PASS
Q2_PROGRAM_GATE = PASS
FATAL_MODEL_MISMATCH = NO
REOPEN_REQUIRED = NO
```

## 2. Q2 停止规则的可追溯证据

`tests/q2_program_gate.py::_run_actual_bootstrap` 现在无条件执行 `END_TO_END_MAX_BOOTSTRAP_CYCLES = 20` 个 FY04/FY03 完整宏周期。固定排程结束后才由离线评估器计算终态误差；在线层没有读取“仿真真值到理想点的距离”来提前停止。

本次 Gate 的机械审计证据来自 `results/q2/q2_program_gate.json`：

| 字段 | 实际值 | 判定 |
| --- | ---: | --- |
| 固定宏周期 | 20 | PASS |
| `for range(END_TO_END_MAX_BOOTSTRAP_CYCLES)` 数 | 1 | PASS |
| `break` 数 | 0 | PASS |
| 真值驱动阶段切换 | `False` | PASS |
| 跨接收机残差汇总 | `False` | PASS |
| 端到端案例 | 33 | 全部 PASS |
| 实际建锚终点用于跟随者 | `True` | PASS |
| 最坏节点误差 | `5.661920698×10⁻¹⁰ d*` | 小于阈值 |
| 最坏边长误差 | `5.647680101×10⁻¹⁰ d*` | 小于阈值 |
| 最大共线距离 | `1.702249231×10⁻¹⁰ d*` | 小于阈值 |
| 最大留出角残差 | `2.515783137×10⁻¹⁰ rad` | 小于阈值 |

在线控制器仍只处理本机角残差、本机试探位移和本机历史；仿真真值、全局几何验收和离线完整候选坐标证书均不回灌动作。33 个案例及上述数值只说明可信基线、目标邻域、非退化条件下的确定性仿真回放，不能推出全局收敛、全局唯一、任意初态成功或现实飞行精度。

## 3. 论文集中修订内容

输出副本为 `paper_drafts/2022国赛B题论文_全篇审查修订版.docx`；用户提供的 `paper_drafts/2022国赛B题论文_学术矢量图版_最终.docx` 只作为输入，未被覆盖。

已完成的非语义/表述性同步包括：

1. 更正 Q1(3) FY04/FY07 目标坐标为 \(B^*=(-50,50\sqrt3)\)、\(C^*=(-50,-50\sqrt3)\)，并说明二者与 FY01 的相位关系为 120°/240°；删除“四架无人机将圆三等分”的错误解释。
2. 补足 Q1(1) 的双侧定夹角圆、任意两约束所有交点、三条原始角回代、局部秩与拒绝语义；FY00/FY01/FY02 仅保留为示例编号。
3. 修正 Q1(2) 节号为 5.2.3、5.2.4、5.2.5，并加入 m=0、392 次枚举、350 条候选、294 条局部域外候选、20°、0.2783389151 rad、局部奇异值及 m=2 的 42 个排列证据表。
4. 统一说明 Q1(3)/Q2 周期映射的零谱半径只属于非零幂零线性化的目标邻域一阶局部收缩；不再写成单周期精确到达或全局保证。
5. 明确 Q2 可信 FY11/FY15 基线是附加尺度条件，保留平移、旋转和镜像规范自由度；完整候选的坐标证书只用于离线判定，不进入在线控制器。
6. 以输入—状态—决策—输出—难点—继承/新增结构—验证的逻辑重写问题分析，补充按观测/几何/控制/验收分类的假设与符号说明，并在参考文献前增加结论。
7. 删除论文中的完整代码转录，改为实际存在的 `src/`、`tests/`、`results/`、`model_contract/` 文件清单；不再虚构 `code/q2_*.py` 或声称 Q1 无单独代码。

## 4. 文献与公式审计

文献仅限仓库既有文献卡和已列参考文献，未进行新检索。对正文引用 [4]、[5]、[7]、[10]、[11]、[12]、[13]，现有材料不足以逐项补全或外部验证的卷期页码已标记为“投稿前人工核验”；没有补造书目信息。

论文正文保留 \(\mathrm{atan2}\) 的语义表达，修订段不改变既有公式、数值或代码/JSON 字段。未发现由本轮新增的异常控制字符或代码字段改名。

## 5. 版式与隐私审计

源稿存在 7 个行内图与 1 个页脚锚定图；修订副本保留七幅正文图，新增的表格均使用统一网格表样式，完整代码附录已删除。已清除输出 DOCX 的核心作者/最后修改者/自定义属性、批注部件和人员部件，并再次运行 `privacy_scrub.py`。

已使用 LibreOffice 重新导出 `paper_drafts/2022国赛B题论文_全篇审查修订版.pdf`，并以文档技能的渲染器生成 36 页 PNG。逐页检查确认：七幅正文图、表格、结论、参考文献与支撑材料清单均未出现裁切或重叠；完整代码附录没有残留。PDF 与 DOCX 均可作为本轮人工核查输入。

## 6. 后续人工核查

人工应重点确认：固定 20 宏周期不再以真值提前停止；Q1(3) 坐标与 120°解释；Q1(1)/Q1(2) 局部结论边界；Q2 可信基线与离线证书披露；以及源稿图形、表格和公式在 Word/PDF 中的逐页可读性。

```text
FINAL_PAPER_HUMAN_VERDICT = PENDING
FINAL_PAPER_FREEZE = PENDING
```
